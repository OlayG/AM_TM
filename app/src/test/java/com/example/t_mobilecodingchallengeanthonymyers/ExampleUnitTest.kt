package com.example.t_mobilecodingchallengeanthonymyers

import org.junit.Test

import org.junit.Assert.*

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }

    /*
    * Given more time I would implement unit tests that check
    * the structure of the collections that store the data retrieved
    * from the API. This would include mocking data and checking if
    * it gets stored in shared preferences properly. Along with checking
    * to see if the conversion from JSON strings and back to objects is
    * working as expected.
    * */

}